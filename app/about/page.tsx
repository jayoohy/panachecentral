import { ComingSoonPage } from "@/components/shared/ComingSoonPage";

// Remove when this page has real content.
export const metadata = { robots: { index: false } };

export default function AboutPage() {
  return <ComingSoonPage />;
}
